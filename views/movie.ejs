<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= movie.title %></title>
    <link rel="stylesheet" href="/css/movie_details.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/welcome.css">
    <style>
        body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #0f0f1a;
    color: white;
}

.movie-details-container {
    display: flex;
    padding: 40px;
    gap: 40px;
    flex-wrap: wrap;
}

.poster img {
    width: 280px;
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(0,0,0,0.5);
}

.details {
    flex: 1;
    max-width: 600px;
}

.details h1 {
    font-size: 36px;
    margin-bottom: 10px;
}

.meta {
    color: #ffcc00;
    font-size: 16px;
    margin-bottom: 20px;
}

.description {
    line-height: 1.6;
    color: #ccc;
}

.shows-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 15px;
    margin-top: 20px;
}

.show-card {
    background: #1f1f3d;
    padding: 15px;
    border-radius: 10px;
    text-align: center;
    border: 1px solid rgba(255,255,255,0.08);
}

.show-card:hover {
    transform: scale(1.05);
    transition: 0.3s;
}

.btn-book {
    display: inline-block;
    margin-top: 10px;
    padding: 8px 12px;
    background: #ff3d00;
    color: white;
    text-decoration: none;
    border-radius: 5px;
    font-size: 14px;
}
.btn-book:hover {
    background: #e63600;
    transition: 0.3s;
}

    </style>
</head>
<body>

<%- include("required/navbar.ejs") %>

<section class="movie-details-container">

    <div class="poster">
        <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
    </div>

    <div class="details">
        <h1><%= movie.title %></h1>
        <p class="meta">
            🎭 <%= movie.language %>  
            ⏱ <%= movie.duration %> mins
        </p>

        <p class="description">
            <%= movie.description %>
        </p>

        <h3>🕒 Available Shows</h3>

        <% if (shows.length === 0) { %>
            <p>No shows available</p>
        <% } %>

        <div class="shows-grid">
            <% shows.forEach(show => { %>
                <div class="show-card">
                    <p>📅 <%= show.show_date %></p>
                    <p>⏰ <%= show.show_time %></p>
                    <p>💰 ₹<%= show.price %></p>

                    <a href="/shows/<%= show.id %>/seats" class="btn-book">
                        Book Seats
                    </a>
                </div>
            <% }) %>
        </div>

    </div>

</section>

<%- include("required/footer.ejs") %>

</body>
</html>
