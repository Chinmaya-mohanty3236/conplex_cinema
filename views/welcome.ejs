<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="/css/welcome.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" 
    integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" 
    crossorigin="anonymous" 
    referrerpolicy="no-referrer" />
</head>
<body>
    <div class="page-wrap">
    <%- include("required/navbar.ejs") %>
    <section class="hero">
        <h2>Book Your Movie Tickets Online</h2>
        <p>3 Screens • Comfortable Seats • Premium Experience 🍿</p>
        <a href="movies" class="btn btn-primary big-btn">🎟️ Book Now</a>
    </section>

    <section id="movies" class="movies-section">
        <h2>🎥 Now Showing</h2>

        <div class="movie-grid">
            <% result.forEach(movie => { %>
                <div class="movie-card">
                    <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
                    <h3><%= movie.title %></h3>
                    <p><%= movie.language %> | <%= movie.duration %> mins</p>
                    <a href="/movies/<%= movie.id %>" class="btn btn-primary">View Shows</a>
                </div>
            <% }) %>
        </div>
    </section>
    <section id="upcoming_movies" class="movies-section">
        <h2>UpComing</h2>
        <div class="movie-grid">
            <% result2.forEach(movie => { %>
                <div class="movie-card">
                    <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
                    <h3><%= movie.title %></h3>
                    <p><%= movie.language %> | <%= movie.duration %> mins</p>
                    <a href="/movies/<%= movie.id %>" class="btn btn-primary">View Shows</a>
                    <span class="release-date"><%= movie.release_date %></span>
                </div>
            <% }) %>
        </div>
    </section>
    </div>
    <hr>
    <%- include("required/footer.ejs") %>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" 
integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" 
crossorigin="anonymous"></script>
</html>